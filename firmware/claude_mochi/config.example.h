// Copie para config.h (ao lado deste arquivo) e preencha — so precisa no
// modo Wi-Fi (LINK_WIFI 1 no topo do .ino). config.h esta no .gitignore,
// entao a senha nunca vai para o repositorio.
#define WIFI_SSID     "sua-rede-wifi"
#define WIFI_PASSWORD "sua-senha"
#define MDNS_NAME     "mochi"   // http://mochi.local
